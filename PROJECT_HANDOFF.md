# 모아루 프로젝트 인수인계

## 새 작업에서 가장 먼저 할 일

이 파일을 끝까지 읽고 현재 소스를 기준으로 이어서 작업한다. 과거 `토리`, `미니톡`, `마이톡` 명칭은 호환 경로나 오래된 주석을 제외하면 사용자 화면에 다시 사용하지 않는다. 현재 제품명은 **모아루**다.

## 현재 기준

- 기준 버전: `3.44.0-server-source-integration` / 배포 표기 v64
- 현재 폴더가 웹 앱 전체 소스다.
- 원본 작업 폴더: `C:\Users\USER\Documents\Codex\2026-08-11\dka\work\mini-talk-current\mini-talk-polished-tori-chat-audited2`
- 이관된 새 프로젝트: `C:\Users\USER\Documents\Codex\Moaru`
- 사용자는 v58 이후 수정본을 아직 실제 배포하지 않았다.
- v58 이후 전체 적용 묶음은 `_release/v60/moaru-v60-all-from-v58-20260817-192653.zip`에 있다.
- v61 누적 전체 적용 묶음은 `_release/v61/moaru-v61-all-from-v60-20260817-195121.zip`에 있다.
- v62 누적 전체 적용 묶음은 `_release/v62/moaru-v62-all-from-v61-20260817-202026.zip`에 있다.
- v63 누적 전체 적용 묶음은 `_release/v63/moaru-v63-all-from-v62-20260817-203240.zip`에 있다.
- v58 기준 v64 누적 교체본은 `delivery/moaru-v64-from-v58-20260817`에 GitHub 웹·Apps Script 8개·Firebase 규칙으로 분리되어 있다.

## 배포 구성

### GitHub Pages

웹 앱 전체 소스를 GitHub 저장소 최상단에 덮어쓴다. 최신 v63 웹 ZIP은 누적 완전체이므로 이전 버전을 별도로 적용하지 않는다.

### Google Apps Script

- `docs/apps-script/Code.gs`: 전체 교체본
- `docs/apps-script/coin-shopping-extension.gs`: 전체 교체본
- 나머지 기존 `.gs` 파일은 삭제하지 않는다.
- 두 파일 교체 후 웹 앱을 새 버전으로 다시 배포해야 한다.
- 로그인과 사용자별 코인은 기존 Google Sheets 기반이다.
- 상품 카탈로그, 보관함, 선물, 관리자 명령 큐는 Apps Script 서버 저장을 사용한다.
- 대화방·메시지·프로필·접속 상태의 실시간 원본은 Firebase이며 Google Sheets는 백업용이다.
- 관리자 지정 과제 원본은 Apps Script 문서 속성이고 `모아루_과제백업` 시트는 읽지 않는 추가 백업이다. 완료 48시간 후 서버 원본만 삭제한다.
- 계정 생성 시 `보상` 시트의 코인 계정도 같은 잠금 구간에서 자동 생성하며, 실패하면 로그인 행을 되돌린다.

### Firebase

- 적용 규칙: `database.rules.json`
- Realtime Database에서 대화방·메시지·프로필·알림 관련 실시간 데이터를 읽는다.
- Firebase Authentication의 익명 로그인이 활성화되어 있어야 한다.
- 기존 토리 경로와 모아루 경로의 호환성을 함부로 제거하지 않는다.

## v64 주요 수정

- 실제 `Code.gs`, `coin.gs`, 코인·쇼핑 확장 코드 구조를 대조해 회원가입과 코인 계정 생성을 원자적으로 연결
- 관리자 지정 과제의 서버 원본을 Google Sheet에서 Apps Script 문서 속성으로 이전하고 시트는 이벤트 백업만 유지
- Firebase 구형 호환 채팅 경로도 익명 인증 세션을 요구하도록 잠그고 관리자 과제·명령·쇼핑 경로는 클라이언트 직접 접근 차단
- 사용자 계정·현재 코인만 Google Sheet의 운영 원장으로 유지하고 그 외 시트 백업을 앱의 읽기 원본에서 분리
- 채팅 시트 백업은 기존 `소통`(전체방)·`대화방`(그 외 방)만 사용하며 중복 `미니톡_*백업` 시트를 더 만들지 않음

## v63 주요 수정

- 과제 완료 보상을 큰 코인 아이콘과 `+숫자`만으로 통일하고 `코인` 중복 문구 제거
- 관리자 직접 코인 변경에 양수 지급과 음수 차감을 모두 지원
- 코인 변경 대상 명단의 닉네임 옆에 서버 기준 현재 보유량 표시
- 지급은 금색 팝 애니메이션, 차감은 붉은색 하강·감소 애니메이션과 실제 부호로 구분

## v62 주요 수정

- 관리자 지정 과제에 완료 보상 코인 수량 필수 지정
- 학생 제출 내용 1,000자 제한, 실시간 글자수, 이미지 1개 첨부
- PC·웨일북에서는 학생 작성/관리자 검토를 920×780px 별도 창으로 열고 모바일에서는 큰 반응형 모달 사용
- 관리자 과제 확인 UI에 제출 대기·다시 요청·진행 중·완료 탭 추가
- 관리자 완료 처리 시 서버에서 1회만 코인 지급, 학생에게 완료 상태·코인 애니메이션·알림 전송
- 관리자 100자 피드백 반려 시 학생에게 알림과 `다시!` 상태를 표시하고 수정 제출 허용
- 완료 과제는 48시간 동안만 표시하고 이후 Apps Script 서버 원본에서 삭제(시트 백업은 유지)
- 직접 관리자 코인 보상도 수신자에게 시스템 알림·큰 코인 애니메이션·추가 수량 표시
- 관리자/과제/선물/초대 공통 사용자 명단의 게스트 제외 유지

## v61 주요 수정

- 관리자 대상 사용자에게 1인당 코인 보상 지급 기능 추가
- 코인 보상 수량·관리자 토큰·등록 사용자 여부를 Apps Script 서버에서 재검증
- 관리자 명령과 과제 배정은 Firebase 직접 쓰기를 사용하지 않고 Apps Script 관리자 큐를 거침
- Firebase `commands`·`tasks` 경로를 읽기/쓰기 금지하고 서버 큐만 수신
- 배포 소스에 남아 있던 고정 관리자 코드 제거; Script Properties 직접 설정만 허용
- 관리자·쇼핑 선물·대화방 초대 후보에서 게스트 제외, 초대 API에서도 게스트 ID 거부
- 비밀번호방에 초대된 멤버는 비밀번호 없이 입장 가능함을 기능 테스트로 고정
- `전체` 탭은 참여 중인 대화방만, `그룹` 탭은 참여 여부와 관계없이 모든 그룹방 표시

## v59~v60 주요 수정

- 프로필 이미지와 상태 메시지를 재접속 후에도 서버에서 복원
- Apps Script 서버 기반 보관함·선물·사용 처리
- 선물 수신 알림
- Firebase 인증이 불가능할 때 관리자 공지·과제를 Apps Script 명령 큐로 전달
- 쇼핑 상품 캐시와 초기 미리 불러오기
- PC 팝업 기본 가로폭 축소
- 대화방 배경을 메시지 스크롤과 분리하여 고정
- 상품 구매 시 화면의 이름·설명·가격·개정 시각과 서버 정보를 코인 차감 전에 비교
- 상품 정보가 다르면 구매하지 않고 그때만 카탈로그를 한 번 갱신
- 정상 구매에는 별도 상품 확인 요청이나 주기적 카탈로그 조회를 추가하지 않음

## 현재 검증 결과

- Node 기능·회귀 테스트 30개 통과(관리자 권한·과제 배정/제출/반려/완료/중복 보상 방지/48시간 삭제·코인 양수 지급/음수 차감·Apps Script 8개 결합 포함)
- `tests/static-check.py` 통과
- JavaScript 및 Apps Script 문법 검사 통과
- 실제 로컬 브라우저에서 게스트 진입·쇼핑 제한과 화면 연결 확인
- 360px 화면 가로 넘침과 콘솔 경고·오류 없음
- 배포본의 FCM 개인키 평문 부재 검사 통과

## 테스트 실행

Node가 PATH에 없을 수 있다. Codex 번들 Node 경로를 먼저 확인하거나 workspace dependency 도구를 사용한다.

- 모든 `tests/*.test.js` 실행
- `tests/static-check.py` 실행
- `docs/apps-script/Code.gs`와 `docs/apps-script/coin-shopping-extension.gs` 문법 검사
- UI 변경 시 로컬 서버와 실제 브라우저로 360px 안팎 팝업 폭도 확인

## 작업 원칙

- 새 수정은 이 폴더에서만 진행한다.
- 누더기식 전역 코드를 늘리지 말고 기존 모듈 구조를 유지한다.
- 사용자가 요청한 범위 밖의 Firebase 경로나 기존 토리 호환 동작을 제거하지 않는다.
- 서버 요청과 이미지 용량을 최소화한다.
- 상품 구매·선물·코인 차감은 클라이언트 표시값만 신뢰하지 말고 서버에서 검증한다.
- 배포본을 줄 때는 GitHub 웹 전체본과 필요한 Apps Script/Firebase 전체본을 구분해 명시한다.
- 기존 사용자의 변경 사항이나 배포 상태를 추정하지 말고, 실제 적용 여부를 확인해서 안내한다.

## 다음 새 작업에 보낼 첫 메시지 예시

> `C:\Users\USER\Documents\Codex\Moaru` 프로젝트를 이어서 작업해줘. 먼저 `PROJECT_HANDOFF.md`를 끝까지 읽고 현재 v60 소스와 테스트 상태를 확인한 뒤 진행해.
